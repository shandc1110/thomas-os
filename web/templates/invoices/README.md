# Invoice templates

`chosen-by-chloe-invoice-reference.docx` is the Chosen by Chloe invoice visual/content
reference (Education sample for Little Lionsheart).

**Runtime invoices are PDF** generated with `@react-pdf/renderer`
(`components/pdf/InvoiceDocument.tsx`). This DOCX is not rendered at runtime and is
not used for education-discount logic.
